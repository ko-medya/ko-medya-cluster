apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lb
  labels:
    {{- include "lb.labels" . | nindent 4 }}
  annotations:
    dev.okteto.com/generate-host: "true"
spec:
  rules:
  - host: lb-ko-medya.cloud.okteto.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lb
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lb
            port:
              number: 443
  - host: galera-ko-medya.cloud.okteto.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: galera
            port:
              number: 3306
